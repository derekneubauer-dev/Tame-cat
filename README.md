# Tame-cat

Tame-cat time clock aims to be the simplest way to track your development. *start*, *stop*, *report*, and check *status*.

It creates a log file in a project directory and scans project source files.
